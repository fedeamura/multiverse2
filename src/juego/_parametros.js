const canvasRows = 13;
const mapaRows = 201;
const width = 40;

const Parametros = {
  seedAltura: 0,
  seedHumedad: 0,
  mensajeDuracion: 5000,
  canvasItemWidth: width,
  canvasItemHeight: width,
  canvasRows: canvasRows,
  canvasCols: canvasRows,
  mapaRows: mapaRows,
  mapaCols: mapaRows
};

export default Parametros;
